# VoyYa Admin — variables de entorno (Vite expone al bundle solo el prefijo VITE_*)
# Copia este archivo a `.env.local` (o `.env`) y ajusta según tu entorno. NUNCA subas el real.

# URL base de la API VoyYa (NestJS) — incluye esquema, sin slash final.
VITE_API_URL=http://localhost:3000
